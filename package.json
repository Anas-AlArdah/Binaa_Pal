{
  "scripts": {
    "client:install": "npm --prefix client ci",
    "client:build": "npm --prefix client run build",
    "server:install": "npm --prefix server ci",
    "server:start": "npm --prefix server start",
    "server:migrate": "npm --prefix server run db:migrate"
  }
}
